import Typography from "@mui/material/Typography";

export default function Footer() {
  return (
    <Typography
      variant="h6"
      component="footer"
      sx={{
        marginTop: "auto",
        paddingTop: "20px",
        paddingBottom: "10px",
        textAlign: "center",
      }}
    >
      X1Z53
    </Typography>
  );
}
